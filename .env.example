# ============================================
# TaskFlow Backend - Variables de entorno
# ============================================
# Railway inyecta DATABASE_URL automáticamente al agregar PostgreSQL.
# Para desarrollo local, usa las variables DB_* individuales.

# --- Entorno ---
NODE_ENV=production
# --- Base de datos ---
# Railway: DATABASE_URL se configura automáticamente
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
# Local:
DB_HOST=localhost
DB_PORT=5432
DB_NAME=task_manager
DB_USER=postgres
DB_PASSWORD=postgres

# --- JWT ---
JWT_SECRET=CAMBIAR_POR_SECRETO_LARGO_Y_ALEATORIO
JWT_EXPIRES_IN=7d

# --- Email (SMTP) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# --- Google OAuth ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=https://tu-backend.up.railway.app/api/auth/google/callback

# --- Outlook OAuth ---
OUTLOOK_CLIENT_ID=
OUTLOOK_CLIENT_SECRET=
OUTLOOK_CALLBACK_URL=https://tu-backend.up.railway.app/api/auth/outlook/callback

# --- Frontend URL (tu dominio de Vercel) ---
FRONTEND_URL=https://tu-app.vercel.app
